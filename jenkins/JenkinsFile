#!/usr/bin/env groovy
pipeline {
    agent {
        docker { image 'maven' }
    }
    stages {
        stage('Build') {
            steps {
                sh "mvn package -Dmaven.test.skip=true"
            }
        }
    }
}